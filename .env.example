# Firebase Service Account Key
# Firebase Console에서 프로젝트 설정 > 서비스 계정에서 새 비공개 키 생성
# 생성된 JSON 파일의 내용을 한 줄로 변환하여 입력
SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id",...}

# Server Port (Optional)
PORT=3000
